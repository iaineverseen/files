local lib = {}

local cloneref = (cloneref or function(x) return x end)
local UserInputService = cloneref(game:GetService("UserInputService"))
local TweenService = cloneref(game:GetService("TweenService"))
local CoreGui = cloneref(game:GetService("CoreGui"))
local RunService = cloneref(game:GetService("RunService"))

local PresetColor = Color3.fromRGB(0, 170, 255)
local CloseBind = Enum.KeyCode.RightControl

-- Modern color palette with better contrast
local ModernColors = {
    Background = Color3.fromRGB(25, 25, 30),
    Secondary = Color3.fromRGB(35, 35, 40),
    Card = Color3.fromRGB(40, 40, 45),
    Accent = Color3.fromRGB(0, 170, 255),
    Success = Color3.fromRGB(85, 220, 115),
    Text = Color3.fromRGB(245, 245, 245),
    SubText = Color3.fromRGB(170, 170, 170),
    Border = Color3.fromRGB(55, 55, 60),
    Hover = Color3.fromRGB(50, 50, 55)
}

if CoreGui:FindFirstChild("ModernLib") then
    CoreGui:FindFirstChild("ModernLib"):Destroy()
end

local ui = Instance.new("ScreenGui")
ui.Name = "ModernLib"
ui.Parent = CoreGui
ui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ui.ResetOnSpawn = false

local DeviceType = UserInputService.TouchEnabled and "Mobile" or "PC"

-- Enhanced mobile toggle
local MobileToggleButton
if DeviceType == "Mobile" then
    MobileToggleButton = Instance.new("ImageButton")
    MobileToggleButton.Name = "MobileToggle"
    MobileToggleButton.Size = UDim2.new(0, 52, 0, 52)
    MobileToggleButton.Position = UDim2.new(1, -65, 0, 25)
    MobileToggleButton.AnchorPoint = Vector2.new(1, 0)
    MobileToggleButton.BackgroundColor3 = ModernColors.Background
    MobileToggleButton.Image = "rbxassetid://3926305904"
    MobileToggleButton.ImageRectOffset = Vector2.new(964, 324)
    MobileToggleButton.ImageRectSize = Vector2.new(36, 36)
    MobileToggleButton.ImageColor3 = ModernColors.Accent
    MobileToggleButton.AutoButtonColor = false
    MobileToggleButton.Parent = ui
    
    local corner = Instance.new("UICorner", MobileToggleButton)
    corner.CornerRadius = UDim.new(1, 0)
    
    local Stroke = Instance.new("UIStroke", MobileToggleButton)
    Stroke.Color = ModernColors.Accent
    Stroke.Thickness = 2
    Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    
    -- Subtle glow
    local Glow = Instance.new("Frame", MobileToggleButton)
    Glow.Name = "Glow"
    Glow.BackgroundColor3 = ModernColors.Accent
    Glow.BackgroundTransparency = 0.9
    Glow.Size = UDim2.new(1.2, 0, 1.2, 0)
    Glow.Position = UDim2.new(-0.1, 0, -0.1, 0)
    Glow.ZIndex = -1
    
    CreateInstance("UICorner", {Parent = Glow, CornerRadius = UDim.new(1, 0)})

    local Dragging, DragInput, DragStart, StartPosition
    
    local function Update(input)
        local Delta = input.Position - DragStart
        local pos = UDim2.new(
            StartPosition.X.Scale,
            StartPosition.X.Offset + Delta.X,
            StartPosition.Y.Scale,
            StartPosition.Y.Offset + Delta.Y
        )
        MobileToggleButton.Position = pos
    end

    MobileToggleButton.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            Dragging = true
            DragStart = input.Position
            StartPosition = MobileToggleButton.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    Dragging = false
                end
            end)
        end
    end)

    MobileToggleButton.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            DragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == DragInput and Dragging then
            Update(input)
        end
    end)
    
    MobileToggleButton.MouseEnter:Connect(function()
        TweenService:Create(MobileToggleButton, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            BackgroundColor3 = ModernColors.Hover,
            Size = UDim2.new(0, 56, 0, 56)
        }):Play()
    end)

    MobileToggleButton.MouseLeave:Connect(function()
        TweenService:Create(MobileToggleButton, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            BackgroundColor3 = ModernColors.Background,
            Size = UDim2.new(0, 52, 0, 52)
        }):Play()
    end)
end

-- Helper functions
local function CreateInstance(className, properties)
    local instance = Instance.new(className)
    for property, value in pairs(properties) do
        instance[property] = value
    end
    return instance
end

local function TweenCreate(instance, tweenInfo, properties)
    return TweenService:Create(instance, tweenInfo, properties)
end

-- Enhanced draggable function
local function MakeDraggable(topbarobject, object)
    local Dragging, DragInput, DragStart, StartPosition

    local function Update(input)
        local Delta = input.Position - DragStart
        local pos = UDim2.new(
            StartPosition.X.Scale,
            StartPosition.X.Offset + Delta.X,
            StartPosition.Y.Scale,
            StartPosition.Y.Offset + Delta.Y
        )
        object.Position = pos
    end

    topbarobject.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            Dragging = true
            DragStart = input.Position
            StartPosition = object.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    Dragging = false
                end
            end)
        end
    end)

    topbarobject.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            DragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == DragInput and Dragging then
            Update(input)
        end
    end)
end

-- Main library function
function lib:Window(text, preset, closebind)
    CloseBind = closebind or CloseBind
    PresetColor = preset or PresetColor
    local fs = false
    
    -- Modern main container
    local Main = CreateInstance("Frame", {
        Name = "Main",
        Parent = ui,
        AnchorPoint = Vector2.new(0.5, 0.5),
        BackgroundColor3 = ModernColors.Background,
        BorderSizePixel = 0,
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Size = UDim2.new(0, 620, 0, 380),
        ClipsDescendants = true,
        Visible = true
    })

    CreateInstance("UICorner", {
        Parent = Main,
        CornerRadius = UDim.new(0, 14)
    })

    CreateInstance("UIStroke", {
        Parent = Main,
        Color = ModernColors.Border,
        Thickness = 1,
        Transparency = 0.6
    })

    -- Header with gradient
    local Header = CreateInstance("Frame", {
        Name = "Header",
        Parent = Main,
        BackgroundColor3 = ModernColors.Secondary,
        Size = UDim2.new(1, 0, 0, 45),
        BorderSizePixel = 0
    })

    CreateInstance("UICorner", {
        Parent = Header,
        CornerRadius = UDim.new(0, 14)
    })

    local HeaderCornerFix = CreateInstance("Frame", {
        Name = "HeaderCornerFix",
        Parent = Header,
        BackgroundColor3 = ModernColors.Secondary,
        Position = UDim2.new(0, 0, 1, 0),
        Size = UDim2.new(1, 0, 0, 14),
        BorderSizePixel = 0
    })

    -- Enhanced tab holder
    local TabHold = CreateInstance("ScrollingFrame", {
        Name = "TabHold",
        Parent = Main,
        BackgroundColor3 = ModernColors.Secondary,
        BackgroundTransparency = 0,
        Position = UDim2.new(0.02, 0, 0.14, 0),
        Size = UDim2.new(0, 130, 0, 300),
        CanvasSize = UDim2.new(0, 0, 0, 0),
        ScrollBarThickness = 0,
        ClipsDescendants = true
    })

    CreateInstance("UICorner", {
        Parent = TabHold,
        CornerRadius = UDim.new(0, 10)
    })

    local TabHoldLayout = CreateInstance("UIListLayout", {
        Parent = TabHold,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, 8)
    })

    TabHoldLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        TabHold.CanvasSize = UDim2.new(0, 0, 0, TabHoldLayout.AbsoluteContentSize.Y)
    end)

    -- Modern title
    local Title = CreateInstance("TextLabel", {
        Name = "Title",
        Parent = Header,
        BackgroundTransparency = 1,
        Position = UDim2.new(0.04, 0, 0, 0),
        Size = UDim2.new(0.5, 0, 1, 0),
        Font = Enum.Font.GothamSemibold,
        Text = text,
        TextColor3 = ModernColors.Accent,
        TextSize = 18,
        TextXAlignment = Enum.TextXAlignment.Left
    })

    local DragFrame = CreateInstance("Frame", {
        Name = "DragFrame",
        Parent = Header,
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 1, 0)
    })

    local TabFolder = CreateInstance("Folder", {
        Name = "TabFolder",
        Parent = Main
    })

    MakeDraggable(DragFrame, Main)

    local uitoggled = false
    
    if DeviceType == "Mobile" and MobileToggleButton then
        MobileToggleButton.MouseButton1Click:Connect(function()
            uitoggled = not uitoggled
            
            if uitoggled then
                Main.Visible = false
                MobileToggleButton.ImageColor3 = ModernColors.SubText
            else
                Main.Visible = true
                MobileToggleButton.ImageColor3 = ModernColors.Accent
            end
        end)
    end

    UserInputService.InputBegan:Connect(function(input, processed)
        if not processed and input.KeyCode == CloseBind then
            uitoggled = not uitoggled
            
            if uitoggled then
                Main.Visible = false
                if DeviceType == "Mobile" and MobileToggleButton then
                    MobileToggleButton.ImageColor3 = ModernColors.SubText
                end
            else
                Main.Visible = true
                if DeviceType == "Mobile" and MobileToggleButton then
                    MobileToggleButton.ImageColor3 = ModernColors.Accent
                end
            end
        end
    end)

    local tabhold = {}
    
    function tabhold:Tab(text)
        local TabBtn = CreateInstance("TextButton", {
            Name = "TabBtn",
            Parent = TabHold,
            BackgroundColor3 = ModernColors.Card,
            Size = UDim2.new(0, 120, 0, 36),
            AutoButtonColor = false,
            Font = Enum.Font.SourceSans,
            Text = "",
            TextColor3 = Color3.fromRGB(0, 0, 0),
            TextSize = 14
        })

        CreateInstance("UICorner", {
            Parent = TabBtn,
            CornerRadius = UDim.new(0, 8)
        })

        local TabTitle = CreateInstance("TextLabel", {
            Name = "TabTitle",
            Parent = TabBtn,
            BackgroundTransparency = 1,
            Size = UDim2.new(1, 0, 1, 0),
            Font = Enum.Font.GothamSemibold,
            Text = text,
            TextColor3 = ModernColors.SubText,
            TextSize = 13
        })

        local TabHighlight = CreateInstance("Frame", {
            Name = "TabHighlight",
            Parent = TabBtn,
            BackgroundColor3 = ModernColors.Accent,
            BorderSizePixel = 0,
            Position = UDim2.new(0, 0, 1, -3),
            Size = UDim2.new(1, 0, 0, 3),
            BackgroundTransparency = 1
        })

        CreateInstance("UICorner", {
            Parent = TabHighlight,
            CornerRadius = UDim.new(0, 2)
        })

        local Tab = CreateInstance("ScrollingFrame", {
            Name = "Tab",
            Parent = TabFolder,
            Active = true,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Position = UDim2.new(0.25, 0, 0.14, 0),
            Size = UDim2.new(0, 440, 0, 300),
            CanvasSize = UDim2.new(0, 0, 0, 0),
            ScrollBarThickness = 3,
            ScrollBarImageColor3 = ModernColors.Accent,
            Visible = false
        })

        local TabLayout = CreateInstance("UIListLayout", {
            Parent = Tab,
            SortOrder = Enum.SortOrder.LayoutOrder,
            Padding = UDim.new(0, 10)
        })

        TabLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            Tab.CanvasSize = UDim2.new(0, 0, 0, TabLayout.AbsoluteContentSize.Y)
        end)

        if not fs then
            fs = true
            TabTitle.TextColor3 = ModernColors.Accent
            TabBtn.BackgroundColor3 = ModernColors.Hover
            TabHighlight.BackgroundTransparency = 0
            Tab.Visible = true
        end

        TabBtn.MouseButton1Click:Connect(function()
            for _, v in pairs(TabFolder:GetChildren()) do
                if v.Name == "Tab" then
                    v.Visible = false
                end
            end
            Tab.Visible = true
            
            for _, v in pairs(TabHold:GetChildren()) do
                if v.Name == "TabBtn" then
                    TweenCreate(v.TabTitle, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        TextColor3 = ModernColors.SubText
                    }):Play()
                    TweenCreate(v, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Card
                    }):Play()
                    TweenCreate(v.TabHighlight, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundTransparency = 1
                    }):Play()
                end
            end
            
            TweenCreate(TabTitle, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                TextColor3 = ModernColors.Accent
            }):Play()
            TweenCreate(TabBtn, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                BackgroundColor3 = ModernColors.Hover
            }):Play()
            TweenCreate(TabHighlight, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                BackgroundTransparency = 0
            }):Play()
        end)

        TabBtn.MouseEnter:Connect(function()
            if TabTitle.TextColor3 ~= ModernColors.Accent then
                TweenCreate(TabBtn, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Hover
                }):Play()
            end
        end)

        TabBtn.MouseLeave:Connect(function()
            if TabTitle.TextColor3 ~= ModernColors.Accent then
                TweenCreate(TabBtn, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Card
                }):Play()
            end
        end)

        local tabcontent = {}
        
        function tabcontent:Label(text)
            local Label = CreateInstance("TextLabel", {
                Name = "Label",
                Parent = Tab,
                BackgroundTransparency = 1,
                Size = UDim2.new(1, 0, 0, 30),
                Font = Enum.Font.GothamSemibold,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Center
            })

            local Divider = CreateInstance("Frame", {
                Name = "Divider",
                Parent = Label,
                BackgroundColor3 = ModernColors.Accent,
                BorderSizePixel = 0,
                Position = UDim2.new(0.1, 0, 1, -2),
                Size = UDim2.new(0.8, 0, 0, 1),
                BackgroundTransparency = 0.7
            })

            CreateInstance("UICorner", {
                Parent = Divider,
                CornerRadius = UDim.new(1, 0)
            })
        end
        
        function tabcontent:Button(text, callback)
            local Button = CreateInstance("TextButton", {
                Name = "Button",
                Parent = Tab,
                BackgroundColor3 = ModernColors.Card,
                Size = UDim2.new(1, 0, 0, 45),
                AutoButtonColor = false,
                Font = Enum.Font.SourceSans,
                Text = "",
                TextColor3 = Color3.fromRGB(0, 0, 0),
                TextSize = 14
            })

            CreateInstance("UICorner", {
                Parent = Button,
                CornerRadius = UDim.new(0, 10)
            })

            CreateInstance("UIStroke", {
                Parent = Button,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.5
            })

            local ButtonTitle = CreateInstance("TextLabel", {
                Name = "ButtonTitle",
                Parent = Button,
                BackgroundTransparency = 1,
                Size = UDim2.new(1, 0, 1, 0),
                Font = Enum.Font.GothamSemibold,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14
            })

            Button.MouseEnter:Connect(function()
                TweenCreate(Button, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Hover,
                    Size = UDim2.new(1, 0, 0, 47)
                }):Play()
            end)

            Button.MouseLeave:Connect(function()
                TweenCreate(Button, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Card,
                    Size = UDim2.new(1, 0, 0, 45)
                }):Play()
            end)

            Button.MouseButton1Click:Connect(function()
                -- Click animation
                TweenCreate(Button, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Accent,
                    Size = UDim2.new(1, 0, 0, 43)
                }):Play()
                
                wait(0.1)
                
                TweenCreate(Button, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Hover,
                    Size = UDim2.new(1, 0, 0, 47)
                }):Play()
                
                pcall(callback)
            end)
        end

        function tabcontent:Toggle(text, default, callback)
            local toggled = default or false

            local Toggle = CreateInstance("TextButton", {
                Name = "Toggle",
                Parent = Tab,
                BackgroundColor3 = ModernColors.Card,
                Size = UDim2.new(1, 0, 0, 45),
                AutoButtonColor = false,
                Font = Enum.Font.SourceSans,
                Text = "",
                TextColor3 = Color3.fromRGB(0, 0, 0),
                TextSize = 14
            })

            CreateInstance("UICorner", {
                Parent = Toggle,
                CornerRadius = UDim.new(0, 10)
            })

            CreateInstance("UIStroke", {
                Parent = Toggle,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.5
            })

            local ToggleTitle = CreateInstance("TextLabel", {
                Name = "ToggleTitle",
                Parent = Toggle,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.04, 0, 0, 0),
                Size = UDim2.new(0.7, 0, 1, 0),
                Font = Enum.Font.Gotham,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left
            })

            -- Fixed toggle switch
            local ToggleContainer = CreateInstance("Frame", {
                Name = "ToggleContainer",
                Parent = Toggle,
                BackgroundColor3 = ModernColors.Border,
                Position = UDim2.new(0.85, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Size = UDim2.new(0, 50, 0, 26)
            })

            CreateInstance("UICorner", {
                Parent = ToggleContainer,
                CornerRadius = UDim.new(1, 0)
            })

            local ToggleKnob = CreateInstance("Frame", {
                Name = "ToggleKnob",
                Parent = ToggleContainer,
                BackgroundColor3 = Color3.fromRGB(220, 220, 220),
                Position = UDim2.new(0.1, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0, 0.5),
                Size = UDim2.new(0, 20, 0, 20)
            })

            CreateInstance("UICorner", {
                Parent = ToggleKnob,
                CornerRadius = UDim.new(1, 0)
            })

            local ToggleFill = CreateInstance("Frame", {
                Name = "ToggleFill",
                Parent = ToggleContainer,
                BackgroundColor3 = ModernColors.Accent,
                Size = UDim2.new(1, 0, 1, 0),
                BackgroundTransparency = 1
            })

            CreateInstance("UICorner", {
                Parent = ToggleFill,
                CornerRadius = UDim.new(1, 0)
            })

            local function UpdateToggleState()
                if toggled then
                    TweenCreate(ToggleFill, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundTransparency = 0
                    }):Play()
                    TweenCreate(ToggleKnob, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        Position = UDim2.new(0.7, 0, 0.5, 0),
                        BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                    }):Play()
                    TweenCreate(Toggle, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Card
                    }):Play()
                else
                    TweenCreate(ToggleFill, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundTransparency = 1
                    }):Play()
                    TweenCreate(ToggleKnob, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        Position = UDim2.new(0.1, 0, 0.5, 0),
                        BackgroundColor3 = Color3.fromRGB(220, 220, 220)
                    }):Play()
                    TweenCreate(Toggle, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Card
                    }):Play()
                end
                pcall(callback, toggled)
            end

            local function ToggleState()
                toggled = not toggled
                UpdateToggleState()
            end

            Toggle.MouseButton1Click:Connect(ToggleState)

            Toggle.MouseEnter:Connect(function()
                if not toggled then
                    TweenCreate(Toggle, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Hover
                    }):Play()
                end
            end)

            Toggle.MouseLeave:Connect(function()
                if not toggled then
                    TweenCreate(Toggle, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Card
                    }):Play()
                end
            end)

            UpdateToggleState()

            return {
                Set = function(self, state)
                    if toggled ~= state then
                        toggled = state
                        UpdateToggleState()
                    end
                end,
                Get = function(self)
                    return toggled
                end
            }
        end

        function tabcontent:Slider(text, min, max, increment, callback)
            local dragging = false
            increment = increment or 1
            local start = min
            
            local Slider = CreateInstance("TextButton", {
                Name = "Slider",
                Parent = Tab,
                BackgroundColor3 = ModernColors.Card,
                Size = UDim2.new(1, 0, 0, 70),
                AutoButtonColor = false,
                Font = Enum.Font.SourceSans,
                Text = "",
                TextColor3 = Color3.fromRGB(0, 0, 0),
                TextSize = 14
            })

            CreateInstance("UICorner", {
                Parent = Slider,
                CornerRadius = UDim.new(0, 10)
            })

            CreateInstance("UIStroke", {
                Parent = Slider,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.5
            })

            local SliderTitle = CreateInstance("TextLabel", {
                Name = "SliderTitle",
                Parent = Slider,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.04, 0, 0.1, 0),
                Size = UDim2.new(0.6, 0, 0, 20),
                Font = Enum.Font.Gotham,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left
            })

            local SliderValue = CreateInstance("TextLabel", {
                Name = "SliderValue",
                Parent = Slider,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.65, 0, 0.1, 0),
                Size = UDim2.new(0.3, 0, 0, 20),
                Font = Enum.Font.GothamSemibold,
                Text = tostring(start),
                TextColor3 = ModernColors.Accent,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Right
            })

            local SlideContainer = CreateInstance("Frame", {
                Name = "SlideContainer",
                Parent = Slider,
                BackgroundColor3 = ModernColors.Border,
                BorderSizePixel = 0,
                Position = UDim2.new(0.04, 0, 0.6, 0),
                Size = UDim2.new(0.92, 0, 0, 6)
            })

            CreateInstance("UICorner", {
                Parent = SlideContainer,
                CornerRadius = UDim.new(1, 0)
            })

            local ProgressFill = CreateInstance("Frame", {
                Name = "ProgressFill",
                Parent = SlideContainer,
                BackgroundColor3 = ModernColors.Accent,
                BorderSizePixel = 0,
                Size = UDim2.new((start - min) / (max - min), 0, 1, 0)
            })

            CreateInstance("UICorner", {
                Parent = ProgressFill,
                CornerRadius = UDim.new(1, 0)
            })

            local SliderKnob = CreateInstance("Frame", {
                Name = "SliderKnob",
                Parent = SlideContainer,
                BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                Position = UDim2.new((start - min) / (max - min), -8, 0.5, -8),
                AnchorPoint = Vector2.new(0, 0.5),
                Size = UDim2.new(0, 16, 0, 16)
            })

            CreateInstance("UICorner", {
                Parent = SliderKnob,
                CornerRadius = UDim.new(1, 0)
            })

            CreateInstance("UIStroke", {
                Parent = SliderKnob,
                Color = ModernColors.Accent,
                Thickness = 2
            })

            local function formatNumber(value)
                if increment < 1 then
                    return string.format("%.2f", value)
                else
                    return tostring(math.floor(value))
                end
            end

            local currentValue = start
            local lastCallbackValue = start

            local function updateSlider(value)
                value = math.clamp(value, min, max)
                currentValue = value
                local normalizedValue = (value - min) / (max - min)
                local knobPos = UDim2.new(normalizedValue, -8, 0.5, -8)
                local fillSize = UDim2.new(normalizedValue, 0, 1, 0)
                
                TweenService:Create(ProgressFill, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    Size = fillSize
                }):Play()
                TweenService:Create(SliderKnob, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    Position = knobPos
                }):Play()
                
                SliderValue.Text = formatNumber(value)
            end

            local function executeCallback(value)
                if value ~= lastCallbackValue then
                    pcall(callback, value)
                    lastCallbackValue = value
                end
            end

            local function startDrag(input)
                dragging = true
                
                TweenService:Create(Slider, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Hover
                }):Play()
                TweenService:Create(SliderKnob, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    Size = UDim2.new(0, 18, 0, 18)
                }):Play()
                
                local function update(inputPosition)
                    local containerPos = SlideContainer.AbsolutePosition.X
                    local containerSize = SlideContainer.AbsoluteSize.X
                    
                    local x = math.clamp(inputPosition.X - containerPos, 0, containerSize)
                    local percent = x / containerSize
                    local value = min + percent * (max - min)
                    
                    if increment > 0 then
                        value = math.floor(value / increment) * increment
                    end
                    
                    updateSlider(value)
                end
                
                update(input.Position)
                
                local moveConn
                moveConn = UserInputService.InputChanged:Connect(function(i)
                    if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
                        update(i.Position)
                    end
                end)
                
                local endConn
                endConn = UserInputService.InputEnded:Connect(function(i)
                    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                        dragging = false
                        if moveConn then moveConn:Disconnect() end
                        if endConn then endConn:Disconnect() end
                        
                        executeCallback(currentValue)
                        
                        TweenService:Create(Slider, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                            BackgroundColor3 = ModernColors.Card
                        }):Play()
                        TweenService:Create(SliderKnob, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                            Size = UDim2.new(0, 16, 0, 16)
                        }):Play()
                    end
                end)
            end

            SliderKnob.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    startDrag(input)
                end
            end)
            
            SlideContainer.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    startDrag(input)
                end
            end)

            Slider.MouseEnter:Connect(function()
                if not dragging then
                    TweenService:Create(Slider, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Hover
                    }):Play()
                end
            end)

            Slider.MouseLeave:Connect(function()
                if not dragging then
                    TweenService:Create(Slider, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Card
                    }):Play()
                end
            end)

            updateSlider(start)
            executeCallback(start)
            
            return {
                Set = function(self, value)
                    value = math.clamp(value, min, max)
                    if increment > 0 then
                        value = math.floor(value / increment) * increment
                    end
                    updateSlider(value)
                    executeCallback(value)
                end,
                Get = function(self)
                    return currentValue
                end
            }
        end

        function tabcontent:Dropdown(text, list, callback)
            local droptog = false
            local itemcount = 0

            local Dropdown = CreateInstance("Frame", {
                Name = "Dropdown",
                Parent = Tab,
                BackgroundColor3 = ModernColors.Card,
                ClipsDescendants = true,
                Size = UDim2.new(1, 0, 0, 45)
            })

            CreateInstance("UICorner", {
                Parent = Dropdown,
                CornerRadius = UDim.new(0, 10)
            })

            CreateInstance("UIStroke", {
                Parent = Dropdown,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.5
            })

            local DropdownBtn = CreateInstance("TextButton", {
                Name = "DropdownBtn",
                Parent = Dropdown,
                BackgroundTransparency = 1,
                Size = UDim2.new(1, 0, 0, 45),
                Font = Enum.Font.SourceSans,
                Text = "",
                TextColor3 = Color3.fromRGB(0, 0, 0),
                TextSize = 14
            })

            local DropdownTitle = CreateInstance("TextLabel", {
                Name = "DropdownTitle",
                Parent = Dropdown,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.04, 0, 0, 0),
                Size = UDim2.new(0.7, 0, 1, 0),
                Font = Enum.Font.Gotham,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left
            })

            local Arrow = CreateInstance("ImageLabel", {
                Name = "Arrow",
                Parent = Dropdown,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.92, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Size = UDim2.new(0, 20, 0, 20),
                Image = "rbxassetid://3926305904",
                ImageRectOffset = Vector2.new(884, 284),
                ImageRectSize = Vector2.new(36, 36),
                ImageColor3 = ModernColors.SubText
            })

            local DropItemHolder = CreateInstance("ScrollingFrame", {
                Name = "DropItemHolder",
                Parent = Dropdown,
                Active = true,
                BackgroundColor3 = ModernColors.Secondary,
                BorderSizePixel = 0,
                Position = UDim2.new(0.02, 0, 1.05, 0),
                Size = UDim2.new(0.96, 0, 0, 0),
                CanvasSize = UDim2.new(0, 0, 0, 0),
                ScrollBarThickness = 3,
                ScrollBarImageColor3 = ModernColors.Accent,
                ClipsDescendants = true
            })

            CreateInstance("UICorner", {
                Parent = DropItemHolder,
                CornerRadius = UDim.new(0, 8)
            })

            local DropLayout = CreateInstance("UIListLayout", {
                Parent = DropItemHolder,
                SortOrder = Enum.SortOrder.LayoutOrder,
                Padding = UDim.new(0, 5)
            })

            DropdownBtn.MouseButton1Click:Connect(function()
                droptog = not droptog
                
                if droptog then
                    local contentSize = math.min(#list * 35, 140)
                    Dropdown:TweenSize(UDim2.new(1, 0, 0, 45 + contentSize), Enum.EasingDirection.Out, Enum.EasingStyle.Quart, 0.2, true)
                    DropItemHolder.Size = UDim2.new(0.96, 0, 0, contentSize)
                    TweenCreate(Arrow, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        Rotation = 180,
                        ImageColor3 = ModernColors.Accent
                    }):Play()
                else
                    Dropdown:TweenSize(UDim2.new(1, 0, 0, 45), Enum.EasingDirection.Out, Enum.EasingStyle.Quart, 0.2, true)
                    TweenCreate(Arrow, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        Rotation = 0,
                        ImageColor3 = ModernColors.SubText
                    }):Play()
                end
            end)

            for _, v in pairs(list) do
                local Item = CreateInstance("TextButton", {
                    Name = "Item",
                    Parent = DropItemHolder,
                    BackgroundColor3 = ModernColors.Card,
                    Size = UDim2.new(1, 0, 0, 32),
                    AutoButtonColor = false,
                    Font = Enum.Font.Gotham,
                    Text = v,
                    TextColor3 = ModernColors.Text,
                    TextSize = 13
                })

                CreateInstance("UICorner", {
                    Parent = Item,
                    CornerRadius = UDim.new(0, 6)
                })

                Item.MouseEnter:Connect(function()
                    TweenCreate(Item, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Hover
                    }):Play()
                end)

                Item.MouseLeave:Connect(function()
                    TweenCreate(Item, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Card
                    }):Play()
                end)

                Item.MouseButton1Click:Connect(function()
                    droptog = false
                    DropdownTitle.Text = text .. " - " .. v
                    pcall(callback, v)
                    Dropdown:TweenSize(UDim2.new(1, 0, 0, 45), Enum.EasingDirection.Out, Enum.EasingStyle.Quart, 0.2, true)
                    TweenCreate(Arrow, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        Rotation = 0,
                        ImageColor3 = ModernColors.SubText
                    }):Play()
                end)

                DropItemHolder.CanvasSize = UDim2.new(0, 0, 0, DropLayout.AbsoluteContentSize.Y)
            end
        end

        function tabcontent:Textbox(text, callback)
            local Textbox = CreateInstance("Frame", {
                Name = "Textbox",
                Parent = Tab,
                BackgroundColor3 = ModernColors.Card,
                Size = UDim2.new(1, 0, 0, 45)
            })

            CreateInstance("UICorner", {
                Parent = Textbox,
                CornerRadius = UDim.new(0, 10)
            })

            CreateInstance("UIStroke", {
                Parent = Textbox,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.5
            })

            local TextboxTitle = CreateInstance("TextLabel", {
                Name = "TextboxTitle",
                Parent = Textbox,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.04, 0, 0, 0),
                Size = UDim2.new(0.4, 0, 1, 0),
                Font = Enum.Font.Gotham,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left
            })

            local InputFrame = CreateInstance("Frame", {
                Name = "InputFrame",
                Parent = Textbox,
                BackgroundColor3 = ModernColors.Background,
                Position = UDim2.new(0.5, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Size = UDim2.new(0.45, 0, 0.6, 0)
            })

            CreateInstance("UICorner", {
                Parent = InputFrame,
                CornerRadius = UDim.new(0, 6)
            })

            CreateInstance("UIStroke", {
                Parent = InputFrame,
                Color = ModernColors.Border,
                Thickness = 1
            })

            local TextBox = CreateInstance("TextBox", {
                Parent = InputFrame,
                BackgroundTransparency = 1,
                Size = UDim2.new(0.9, 0, 0.9, 0),
                Position = UDim2.new(0.5, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Font = Enum.Font.Gotham,
                Text = "",
                PlaceholderText = "Type here...",
                PlaceholderColor3 = ModernColors.SubText,
                TextColor3 = ModernColors.Text,
                TextSize = 13,
                ClearTextOnFocus = false
            })

            TextBox.Focused:Connect(function()
                TweenCreate(InputFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Hover
                }):Play()
            end)

            TextBox.FocusLost:Connect(function(enterPressed)
                TweenCreate(InputFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Background
                }):Play()
                
                if enterPressed and #TextBox.Text > 0 then
                    pcall(callback, TextBox.Text)
                end
            end)
        end

        return tabcontent
    end
    
    return tabhold
end

return lib
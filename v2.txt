local lib = {}

local cloneref = (cloneref or function(x) return x end)
local UserInputService = cloneref(game:GetService("UserInputService"))
local TweenService = cloneref(game:GetService("TweenService"))
local CoreGui = cloneref(game:GetService("CoreGui"))
local RunService = cloneref(game:GetService("RunService"))

local PresetColor = Color3.fromRGB(44, 120, 224)
local CloseBind = Enum.KeyCode.RightControl

-- Modern color palette
local ModernColors = {
    Background = Color3.fromRGB(30, 30, 35),
    Secondary = Color3.fromRGB(40, 40, 45),
    Accent = Color3.fromRGB(44, 120, 224),
    Text = Color3.fromRGB(240, 240, 240),
    SubText = Color3.fromRGB(180, 180, 180),
    Border = Color3.fromRGB(60, 60, 65)
}

if CoreGui:FindFirstChild("67Library") then
    CoreGui:FindFirstChild("67Library"):Destroy()
end

local ui = Instance.new("ScreenGui")
ui.Name = "67Library"
ui.Parent = CoreGui
ui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ui.ResetOnSpawn = false

local DeviceType = UserInputService.TouchEnabled and "Mobile" or "PC"

-- Enhanced mobile toggle with modern design
local MobileToggleButton
if DeviceType == "Mobile" then
    MobileToggleButton = Instance.new("ImageButton")
    MobileToggleButton.Name = "MobileToggle"
    MobileToggleButton.Size = UDim2.new(0, 50, 0, 50)
    MobileToggleButton.Position = UDim2.new(1, -60, 0, 20)
    MobileToggleButton.AnchorPoint = Vector2.new(1, 0)
    MobileToggleButton.BackgroundColor3 = ModernColors.Background
    MobileToggleButton.Image = "rbxassetid://3926305904"
    MobileToggleButton.ImageRectOffset = Vector2.new(964, 324)
    MobileToggleButton.ImageRectSize = Vector2.new(36, 36)
    MobileToggleButton.ImageColor3 = ModernColors.Accent
    MobileToggleButton.AutoButtonColor = false
    MobileToggleButton.Parent = ui
    
    local corner = Instance.new("UICorner", MobileToggleButton)
    corner.CornerRadius = UDim.new(1, 0)
    
    local Stroke = Instance.new("UIStroke", MobileToggleButton)
    Stroke.Color = ModernColors.Accent
    Stroke.Thickness = 2
    Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    
    -- Add glow effect
    local Glow = Instance.new("ImageLabel", MobileToggleButton)
    Glow.Name = "Glow"
    Glow.BackgroundTransparency = 1
    Glow.Size = UDim2.new(1.2, 0, 1.2, 0)
    Glow.Position = UDim2.new(-0.1, 0, -0.1, 0)
    Glow.ZIndex = -1
    Glow.Image = "rbxassetid://4996891970"
    Glow.ImageColor3 = ModernColors.Accent
    Glow.ScaleType = Enum.ScaleType.Slice
    Glow.SliceCenter = Rect.new(49, 49, 450, 450)
    Glow.ImageTransparency = 0.8
    
    local Dragging, DragInput, DragStart, StartPosition
    
    local function Update(input)
        local Delta = input.Position - DragStart
        local pos = UDim2.new(
            StartPosition.X.Scale,
            StartPosition.X.Offset + Delta.X,
            StartPosition.Y.Scale,
            StartPosition.Y.Offset + Delta.Y
        )
        MobileToggleButton.Position = pos
    end

    MobileToggleButton.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            Dragging = true
            DragStart = input.Position
            StartPosition = MobileToggleButton.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    Dragging = false
                end
            end)
        end
    end)

    MobileToggleButton.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            DragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == DragInput and Dragging then
            Update(input)
        end
    end)
    
    MobileToggleButton.MouseEnter:Connect(function()
        TweenService:Create(MobileToggleButton, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            BackgroundColor3 = ModernColors.Secondary,
            Size = UDim2.new(0, 55, 0, 55)
        }):Play()
        TweenService:Create(Stroke, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Thickness = 3
        }):Play()
        TweenService:Create(Glow, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            ImageTransparency = 0.6
        }):Play()
    end)

    MobileToggleButton.MouseLeave:Connect(function()
        TweenService:Create(MobileToggleButton, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            BackgroundColor3 = ModernColors.Background,
            Size = UDim2.new(0, 50, 0, 50)
        }):Play()
        TweenService:Create(Stroke, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Thickness = 2
        }):Play()
        TweenService:Create(Glow, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            ImageTransparency = 0.8
        }):Play()
    end)
end

-- Helper functions
local function CreateInstance(className, properties)
    local instance = Instance.new(className)
    for property, value in pairs(properties) do
        instance[property] = value
    end
    return instance
end

local function TweenCreate(instance, tweenInfo, properties)
    return TweenService:Create(instance, tweenInfo, properties)
end

-- Enhanced draggable function with smooth movement
local function MakeDraggable(topbarobject, object)
    local Dragging, DragInput, DragStart, StartPosition
    local DragSpeed = 0.5

    local function Update(input)
        local Delta = input.Position - DragStart
        local pos = UDim2.new(
            StartPosition.X.Scale,
            StartPosition.X.Offset + Delta.X,
            StartPosition.Y.Scale,
            StartPosition.Y.Offset + Delta.Y
        )
        object.Position = pos
    end

    topbarobject.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            Dragging = true
            DragStart = input.Position
            StartPosition = object.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    Dragging = false
                end
            end)
        end
    end)

    topbarobject.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            DragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == DragInput and Dragging then
            Update(input)
        end
    end)
end

-- Main library function
function lib:Window(text, preset, closebind)
    CloseBind = closebind or CloseBind
    PresetColor = preset or PresetColor
    local fs = false
    
    -- Modern main container with glass effect
    local Main = CreateInstance("Frame", {
        Name = "Main",
        Parent = ui,
        AnchorPoint = Vector2.new(0.5, 0.5),
        BackgroundColor3 = ModernColors.Background,
        BackgroundTransparency = 0.1,
        BorderSizePixel = 0,
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Size = UDim2.new(0, 600, 0, 350),
        ClipsDescendants = true,
        Visible = true
    })

    -- Glass effect
    local GlassEffect = CreateInstance("Frame", {
        Name = "GlassEffect",
        Parent = Main,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 0.9,
        Size = UDim2.new(1, 0, 1, 0),
        ZIndex = -1
    })

    CreateInstance("UICorner", {
        Parent = Main,
        CornerRadius = UDim.new(0, 12)
    })

    CreateInstance("UIStroke", {
        Parent = Main,
        Color = ModernColors.Border,
        Thickness = 1,
        Transparency = 0.8
    })

    -- Enhanced tab holder with modern design
    local TabHold = CreateInstance("ScrollingFrame", {
        Name = "TabHold",
        Parent = Main,
        BackgroundColor3 = ModernColors.Secondary,
        BackgroundTransparency = 0.5,
        Position = UDim2.new(0.02, 0, 0.12, 0),
        Size = UDim2.new(0, 120, 0, 270),
        CanvasSize = UDim2.new(0, 0, 0, 0),
        ScrollBarThickness = 3,
        ScrollBarImageColor3 = ModernColors.Accent,
        ClipsDescendants = true
    })

    CreateInstance("UICorner", {
        Parent = TabHold,
        CornerRadius = UDim.new(0, 8)
    })

    local TabHoldLayout = CreateInstance("UIListLayout", {
        Parent = TabHold,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, 8)
    })

    TabHoldLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        TabHold.CanvasSize = UDim2.new(0, 0, 0, TabHoldLayout.AbsoluteContentSize.Y)
    end)

    -- Modern title with gradient
    local Title = CreateInstance("TextLabel", {
        Name = "Title",
        Parent = Main,
        BackgroundTransparency = 1,
        Position = UDim2.new(0.03, 0, 0.03, 0),
        Size = UDim2.new(0, 200, 0, 25),
        Font = Enum.Font.GothamSemibold,
        Text = text,
        TextColor3 = ModernColors.Accent,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left
    })

    local DragFrame = CreateInstance("Frame", {
        Name = "DragFrame",
        Parent = Main,
        BackgroundTransparency = 1,
        Size = UDim2.new(0, 600, 0, 45)
    })

    local TabFolder = CreateInstance("Folder", {
        Name = "TabFolder",
        Parent = Main
    })

    MakeDraggable(DragFrame, Main)

    local uitoggled = false
    
    if DeviceType == "Mobile" and MobileToggleButton then
        MobileToggleButton.MouseButton1Click:Connect(function()
            uitoggled = not uitoggled
            
            if uitoggled then
                Main.Visible = false
                MobileToggleButton.ImageColor3 = Color3.fromRGB(150, 150, 150)
                TweenService:Create(MobileToggleButton, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Secondary
                }):Play()
            else
                Main.Visible = true
                MobileToggleButton.ImageColor3 = ModernColors.Accent
                TweenService:Create(MobileToggleButton, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Background
                }):Play()
            end
        end)
    end

    UserInputService.InputBegan:Connect(function(input, processed)
        if not processed and input.KeyCode == CloseBind then
            uitoggled = not uitoggled
            
            if uitoggled then
                Main.Visible = false
                if DeviceType == "Mobile" and MobileToggleButton then
                    MobileToggleButton.ImageColor3 = Color3.fromRGB(150, 150, 150)
                end
            else
                Main.Visible = true
                if DeviceType == "Mobile" and MobileToggleButton then
                    MobileToggleButton.ImageColor3 = ModernColors.Accent
                end
            end
        end
    end)

    local tabhold = {}
    
    function tabhold:Tab(text)
        local TabBtn = CreateInstance("TextButton", {
            Name = "TabBtn",
            Parent = TabHold,
            BackgroundTransparency = 1,
            Size = UDim2.new(0, 110, 0, 30),
            Font = Enum.Font.SourceSans,
            Text = "",
            TextColor3 = Color3.fromRGB(0, 0, 0),
            TextSize = 14
        })

        local TabTitle = CreateInstance("TextLabel", {
            Name = "TabTitle",
            Parent = TabBtn,
            BackgroundTransparency = 1,
            Size = UDim2.new(0, 110, 0, 30),
            Font = Enum.Font.GothamSemibold,
            Text = text,
            TextColor3 = ModernColors.SubText,
            TextSize = 13,
            TextXAlignment = Enum.TextXAlignment.Center
        })

        local TabBtnIndicator = CreateInstance("Frame", {
            Name = "TabBtnIndicator",
            Parent = TabBtn,
            BackgroundColor3 = ModernColors.Accent,
            BorderSizePixel = 0,
            Position = UDim2.new(0.5, 0, 1, 0),
            AnchorPoint = Vector2.new(0.5, 0),
            Size = UDim2.new(0, 0, 0, 2)
        })

        CreateInstance("UICorner", {
            Parent = TabBtnIndicator,
            CornerRadius = UDim.new(1, 0)
        })

        local Tab = CreateInstance("ScrollingFrame", {
            Name = "Tab",
            Parent = TabFolder,
            Active = true,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Position = UDim2.new(0.25, 0, 0.12, 0),
            Size = UDim2.new(0, 430, 0, 270),
            CanvasSize = UDim2.new(0, 0, 0, 0),
            ScrollBarThickness = 3,
            ScrollBarImageColor3 = ModernColors.Accent,
            Visible = false
        })

        local TabLayout = CreateInstance("UIListLayout", {
            Parent = Tab,
            SortOrder = Enum.SortOrder.LayoutOrder,
            Padding = UDim.new(0, 8)
        })

        TabLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            Tab.CanvasSize = UDim2.new(0, 0, 0, TabLayout.AbsoluteContentSize.Y)
        end)

        if not fs then
            fs = true
            TabBtnIndicator.Size = UDim2.new(0, 80, 0, 2)
            TabTitle.TextColor3 = ModernColors.Accent
            Tab.Visible = true
        end

        TabBtn.MouseButton1Click:Connect(function()
            for _, v in pairs(TabFolder:GetChildren()) do
                if v.Name == "Tab" then
                    v.Visible = false
                end
            end
            Tab.Visible = true
            
            for _, v in pairs(TabHold:GetChildren()) do
                if v.Name == "TabBtn" then
                    v.TabBtnIndicator:TweenSize(UDim2.new(0, 0, 0, 2), Enum.EasingDirection.Out, Enum.EasingStyle.Quart, 0.2, true)
                    TweenCreate(v.TabTitle, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        TextColor3 = ModernColors.SubText
                    }):Play()
                end
            end
            
            TabBtnIndicator:TweenSize(UDim2.new(0, 80, 0, 2), Enum.EasingDirection.Out, Enum.EasingStyle.Quart, 0.2, true)
            TweenCreate(TabTitle, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                TextColor3 = ModernColors.Accent
            }):Play()
        end)

        local tabcontent = {}
        
        function tabcontent:Label(text)
            local Label = CreateInstance("TextLabel", {
                Name = "Label",
                Parent = Tab,
                BackgroundTransparency = 1,
                Size = UDim2.new(0, 420, 0, 30),
                Font = Enum.Font.Gotham,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Center
            })

            local Underline = CreateInstance("Frame", {
                Name = "Underline",
                Parent = Label,
                BackgroundColor3 = ModernColors.Accent,
                BorderSizePixel = 0,
                Position = UDim2.new(0.1, 0, 1, 0),
                Size = UDim2.new(0.8, 0, 0, 1),
                BackgroundTransparency = 0.7
            })
        end
        
        function tabcontent:Button(text, callback)
            local Button = CreateInstance("TextButton", {
                Name = "Button",
                Parent = Tab,
                BackgroundColor3 = ModernColors.Secondary,
                Size = UDim2.new(0, 420, 0, 45),
                AutoButtonColor = false,
                Font = Enum.Font.SourceSans,
                Text = "",
                TextColor3 = Color3.fromRGB(0, 0, 0),
                TextSize = 14
            })

            CreateInstance("UICorner", {
                Parent = Button,
                CornerRadius = UDim.new(0, 8)
            })

            CreateInstance("UIStroke", {
                Parent = Button,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.8
            })

            local ButtonTitle = CreateInstance("TextLabel", {
                Name = "ButtonTitle",
                Parent = Button,
                BackgroundTransparency = 1,
                Size = UDim2.new(1, 0, 1, 0),
                Font = Enum.Font.GothamSemibold,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14
            })

            local HoverEffect = CreateInstance("Frame", {
                Name = "HoverEffect",
                Parent = Button,
                BackgroundColor3 = ModernColors.Accent,
                BackgroundTransparency = 1,
                Size = UDim2.new(1, 0, 1, 0),
                ZIndex = -1
            })

            CreateInstance("UICorner", {
                Parent = HoverEffect,
                CornerRadius = UDim.new(0, 8)
            })

            Button.MouseEnter:Connect(function()
                TweenCreate(Button, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Secondary:Lerp(ModernColors.Accent, 0.1)
                }):Play()
                TweenCreate(HoverEffect, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundTransparency = 0.9
                }):Play()
            end)

            Button.MouseLeave:Connect(function()
                TweenCreate(Button, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Secondary
                }):Play()
                TweenCreate(HoverEffect, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundTransparency = 1
                }):Play()
            end)

            Button.MouseButton1Click:Connect(function()
                -- Ripple effect
                local Ripple = CreateInstance("Frame", {
                    Parent = Button,
                    BackgroundColor3 = ModernColors.Accent,
                    BackgroundTransparency = 0.7,
                    Size = UDim2.new(0, 0, 0, 0),
                    Position = UDim2.new(0.5, 0, 0.5, 0),
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    ZIndex = -1
                })
                
                CreateInstance("UICorner", {
                    Parent = Ripple,
                    CornerRadius = UDim.new(1, 0)
                })
                
                TweenCreate(Ripple, TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    Size = UDim2.new(2, 0, 2, 0),
                    BackgroundTransparency = 1
                }):Play()
                
                delay(0.6, function()
                    Ripple:Destroy()
                end)
                
                pcall(callback)
            end)
        end

        function tabcontent:Toggle(text, default, callback)
            local toggled = default or false

            local Toggle = CreateInstance("TextButton", {
                Name = "Toggle",
                Parent = Tab,
                BackgroundColor3 = ModernColors.Secondary,
                Size = UDim2.new(0, 420, 0, 45),
                AutoButtonColor = false,
                Font = Enum.Font.SourceSans,
                Text = "",
                TextColor3 = Color3.fromRGB(0, 0, 0),
                TextSize = 14
            })

            CreateInstance("UICorner", {
                Parent = Toggle,
                CornerRadius = UDim.new(0, 8)
            })

            CreateInstance("UIStroke", {
                Parent = Toggle,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.8
            })

            local ToggleTitle = CreateInstance("TextLabel", {
                Name = "ToggleTitle",
                Parent = Toggle,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.05, 0, 0, 0),
                Size = UDim2.new(0.7, 0, 1, 0),
                Font = Enum.Font.Gotham,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left
            })

            -- Modern toggle switch
            local ToggleSwitch = CreateInstance("Frame", {
                Name = "ToggleSwitch",
                Parent = Toggle,
                BackgroundColor3 = ModernColors.Border,
                Position = UDim2.new(0.85, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Size = UDim2.new(0, 50, 0, 25)
            })

            CreateInstance("UICorner", {
                Parent = ToggleSwitch,
                CornerRadius = UDim.new(1, 0)
            })

            local ToggleKnob = CreateInstance("Frame", {
                Name = "ToggleKnob",
                Parent = ToggleSwitch,
                BackgroundColor3 = Color3.fromRGB(240, 240, 240),
                Position = UDim2.new(0.1, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0, 0.5),
                Size = UDim2.new(0, 20, 0, 20)
            })

            CreateInstance("UICorner", {
                Parent = ToggleKnob,
                CornerRadius = UDim.new(1, 0)
            })

            local function UpdateToggleState()
                if toggled then
                    TweenCreate(ToggleSwitch, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Accent
                    }):Play()
                    TweenCreate(ToggleKnob, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        Position = UDim2.new(0.7, 0, 0.5, 0),
                        BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                    }):Play()
                else
                    TweenCreate(ToggleSwitch, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Border
                    }):Play()
                    TweenCreate(ToggleKnob, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        Position = UDim2.new(0.1, 0, 0.5, 0),
                        BackgroundColor3 = Color3.fromRGB(200, 200, 200)
                    }):Play()
                end
                pcall(callback, toggled)
            end

            local function ToggleState()
                toggled = not toggled
                UpdateToggleState()
            end

            Toggle.MouseButton1Click:Connect(ToggleState)

            UpdateToggleState()

            return {
                Set = function(self, state)
                    if toggled ~= state then
                        toggled = state
                        UpdateToggleState()
                    end
                end
            }
        end

        function tabcontent:Slider(text, min, max, increment, callback)
            local dragging = false
            increment = increment or 1
            local start = min
            
            local Slider = CreateInstance("TextButton", {
                Name = "Slider",
                Parent = Tab,
                BackgroundColor3 = ModernColors.Secondary,
                Size = UDim2.new(0, 420, 0, 70),
                AutoButtonColor = false,
                Font = Enum.Font.SourceSans,
                Text = "",
                TextColor3 = Color3.fromRGB(0, 0, 0),
                TextSize = 14
            })

            CreateInstance("UICorner", {
                Parent = Slider,
                CornerRadius = UDim.new(0, 8)
            })

            CreateInstance("UIStroke", {
                Parent = Slider,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.8
            })

            local SliderTitle = CreateInstance("TextLabel", {
                Name = "SliderTitle",
                Parent = Slider,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.05, 0, 0.1, 0),
                Size = UDim2.new(0.6, 0, 0, 20),
                Font = Enum.Font.Gotham,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left
            })

            local SliderValue = CreateInstance("TextLabel", {
                Name = "SliderValue",
                Parent = Slider,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.65, 0, 0.1, 0),
                Size = UDim2.new(0.3, 0, 0, 20),
                Font = Enum.Font.GothamSemibold,
                Text = tostring(start),
                TextColor3 = ModernColors.Accent,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Right
            })

            local SlideFrame = CreateInstance("Frame", {
                Name = "SlideFrame",
                Parent = Slider,
                BackgroundColor3 = ModernColors.Border,
                BorderSizePixel = 0,
                Position = UDim2.new(0.05, 0, 0.6, 0),
                Size = UDim2.new(0.9, 0, 0, 6)
            })

            CreateInstance("UICorner", {
                Parent = SlideFrame,
                CornerRadius = UDim.new(1, 0)
            })

            local CurrentValueFrame = CreateInstance("Frame", {
                Name = "CurrentValueFrame",
                Parent = SlideFrame,
                BackgroundColor3 = ModernColors.Accent,
                BorderSizePixel = 0,
                Size = UDim2.new((start - min) / (max - min), 0, 1, 0)
            })

            CreateInstance("UICorner", {
                Parent = CurrentValueFrame,
                CornerRadius = UDim.new(1, 0)
            })

            local SlideCircle = CreateInstance("Frame", {
                Name = "SlideCircle",
                Parent = SlideFrame,
                BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                Position = UDim2.new((start - min) / (max - min), -8, 0.5, -8),
                AnchorPoint = Vector2.new(0, 0.5),
                Size = UDim2.new(0, 16, 0, 16),
                ZIndex = 2
            })

            CreateInstance("UICorner", {
                Parent = SlideCircle,
                CornerRadius = UDim.new(1, 0)
            })

            CreateInstance("UIStroke", {
                Parent = SlideCircle,
                Color = ModernColors.Accent,
                Thickness = 2
            })

            -- Glow effect for slider circle
            local CircleGlow = CreateInstance("ImageLabel", {
                Name = "CircleGlow",
                Parent = SlideCircle,
                BackgroundTransparency = 1,
                Size = UDim2.new(1.5, 0, 1.5, 0),
                Position = UDim2.new(-0.25, 0, -0.25, 0),
                ZIndex = -1,
                Image = "rbxassetid://4996891970",
                ImageColor3 = ModernColors.Accent,
                ScaleType = Enum.ScaleType.Slice,
                SliceCenter = Rect.new(49, 49, 450, 450),
                ImageTransparency = 0.8
            })

            local function formatNumber(value)
                if math.abs(value) < 0.0001 and value ~= 0 then
                    return string.format("%.2e", value)
                end
                
                if math.abs(value) >= 10000 then
                    local formatted = tostring(math.floor(value))
                    local k = formatted:len() % 3
                    if k == 0 then k = 3 end
                    return formatted:sub(1, k) .. formatted:sub(k+1):gsub("(%d%d%d)", ",%1")
                end
                
                if increment < 1 then
                    local decimalPlaces = 0
                    local tempIncrement = increment
                    
                    while tempIncrement < 1 do
                        decimalPlaces = decimalPlaces + 1
                        tempIncrement = tempIncrement * 10
                        if tempIncrement >= 1 then break end
                    end
                    
                    decimalPlaces = math.min(decimalPlaces + 1, 6)
                    
                    local formatString = "%." .. decimalPlaces .. "f"
                    local formatted = string.format(formatString, value)
                    
                    formatted = formatted:gsub("%.(%d-)0*$", function(decimal)
                        return decimal == "" and "" or "." .. decimal
                    end)
                    formatted = formatted:gsub("%.$", "")
                    
                    return formatted
                else
                    return tostring(math.floor(value))
                end
            end

            local currentValue = start
            local lastCallbackValue = start

            local function updateSlider(value)
                value = math.clamp(value, min, max)
                currentValue = value
                local normalizedValue = (value - min) / (max - min)
                local circlePos = UDim2.new(normalizedValue, -8, 0.5, -8)
                local frameSize = UDim2.new(normalizedValue, 0, 1, 0)
                
                TweenService:Create(CurrentValueFrame, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    Size = frameSize
                }):Play()
                TweenService:Create(SlideCircle, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    Position = circlePos
                }):Play()
                
                SliderValue.Text = formatNumber(value)
            end

            local function executeCallback(value)
                if value ~= lastCallbackValue then
                    pcall(callback, value)
                    lastCallbackValue = value
                end
            end

            local function startDrag(input)
                dragging = true
                
                TweenService:Create(Slider, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Secondary:Lerp(ModernColors.Accent, 0.1)
                }):Play()
                TweenService:Create(CircleGlow, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    ImageTransparency = 0.6
                }):Play()
                
                local function update(inputPosition)
                    local slideFrameAbsolutePos = SlideFrame.AbsolutePosition.X
                    local slideFrameAbsoluteSize = SlideFrame.AbsoluteSize.X
                    
                    local x = math.clamp(inputPosition.X - slideFrameAbsolutePos, 0, slideFrameAbsoluteSize)
                    local percent = x / slideFrameAbsoluteSize
                    local value = min + percent * (max - min)
                    
                    if increment > 0 then
                        value = math.floor(value / increment) * increment
                    end
                    
                    updateSlider(value)
                end
                
                update(input.Position)
                
                local moveConn
                moveConn = UserInputService.InputChanged:Connect(function(i)
                    if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
                        update(i.Position)
                    end
                end)
                
                local endConn
                endConn = UserInputService.InputEnded:Connect(function(i)
                    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                        dragging = false
                        if moveConn then
                            moveConn:Disconnect()
                        end
                        if endConn then
                            endConn:Disconnect()
                        end
                        
                        executeCallback(currentValue)
                        
                        TweenService:Create(Slider, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                            BackgroundColor3 = ModernColors.Secondary
                        }):Play()
                        TweenService:Create(CircleGlow, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                            ImageTransparency = 0.8
                        }):Play()
                    end
                end)
            end

            SlideCircle.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    startDrag(input)
                end
            end)
            
            SlideFrame.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    startDrag(input)
                end
            end)

            Slider.MouseEnter:Connect(function()
                if not dragging then
                    TweenService:Create(Slider, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Secondary:Lerp(ModernColors.Accent, 0.05)
                    }):Play()
                end
            end)

            Slider.MouseLeave:Connect(function()
                if not dragging then
                    TweenService:Create(Slider, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Secondary
                    }):Play()
                end
            end)

            updateSlider(start)
            executeCallback(start)
            
            return {
                Set = function(self, value)
                    value = math.clamp(value, min, max)
                    if increment > 0 then
                        value = math.floor(value / increment) * increment
                    end
                    updateSlider(value)
                    executeCallback(value)
                end,
                Get = function(self)
                    return currentValue
                end
            }
        end

        function tabcontent:Dropdown(text, list, callback)
            local droptog = false
            local framesize = 0
            local itemcount = 0

            local Dropdown = CreateInstance("Frame", {
                Name = "Dropdown",
                Parent = Tab,
                BackgroundColor3 = ModernColors.Secondary,
                ClipsDescendants = true,
                Size = UDim2.new(0, 420, 0, 45)
            })

            CreateInstance("UICorner", {
                Parent = Dropdown,
                CornerRadius = UDim.new(0, 8)
            })

            CreateInstance("UIStroke", {
                Parent = Dropdown,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.8
            })

            local DropdownBtn = CreateInstance("TextButton", {
                Name = "DropdownBtn",
                Parent = Dropdown,
                BackgroundTransparency = 1,
                Size = UDim2.new(0, 420, 0, 45),
                Font = Enum.Font.SourceSans,
                Text = "",
                TextColor3 = Color3.fromRGB(0, 0, 0),
                TextSize = 14
            })

            local DropdownTitle = CreateInstance("TextLabel", {
                Name = "DropdownTitle",
                Parent = Dropdown,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.05, 0, 0, 0),
                Size = UDim2.new(0.7, 0, 1, 0),
                Font = Enum.Font.Gotham,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left
            })

            local ArrowImg = CreateInstance("ImageLabel", {
                Name = "ArrowImg",
                Parent = Dropdown,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.9, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Size = UDim2.new(0, 20, 0, 20),
                Image = "rbxassetid://3926305904",
                ImageRectOffset = Vector2.new(884, 284),
                ImageRectSize = Vector2.new(36, 36),
                ImageColor3 = ModernColors.SubText
            })

            local DropItemHolder = CreateInstance("ScrollingFrame", {
                Name = "DropItemHolder",
                Parent = Dropdown,
                Active = true,
                BackgroundColor3 = ModernColors.Secondary,
                BorderSizePixel = 0,
                Position = UDim2.new(0.05, 0, 1.1, 0),
                Size = UDim2.new(0, 380, 0, 0),
                CanvasSize = UDim2.new(0, 0, 0, 0),
                ScrollBarThickness = 3,
                ScrollBarImageColor3 = ModernColors.Accent,
                ClipsDescendants = true
            })

            CreateInstance("UICorner", {
                Parent = DropItemHolder,
                CornerRadius = UDim.new(0, 8)
            })

            CreateInstance("UIStroke", {
                Parent = DropItemHolder,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.8
            })

            local DropLayout = CreateInstance("UIListLayout", {
                Parent = DropItemHolder,
                SortOrder = Enum.SortOrder.LayoutOrder,
                Padding = UDim.new(0, 5)
            })

            DropdownBtn.MouseButton1Click:Connect(function()
                droptog = not droptog
                
                if droptog then
                    Dropdown:TweenSize(UDim2.new(0, 420, 0, 55 + framesize), Enum.EasingDirection.Out, Enum.EasingStyle.Quart, 0.2, true)
                    TweenCreate(ArrowImg, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        Rotation = 180,
                        ImageColor3 = ModernColors.Accent
                    }):Play()
                else
                    Dropdown:TweenSize(UDim2.new(0, 420, 0, 45), Enum.EasingDirection.Out, Enum.EasingStyle.Quart, 0.2, true)
                    TweenCreate(ArrowImg, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        Rotation = 0,
                        ImageColor3 = ModernColors.SubText
                    }):Play()
                end
            end)

            for _, v in pairs(list) do
                itemcount = itemcount + 1
                if itemcount <= 3 then
                    framesize = framesize + 30
                    DropItemHolder.Size = UDim2.new(0, 380, 0, framesize)
                end

                local Item = CreateInstance("TextButton", {
                    Name = "Item",
                    Parent = DropItemHolder,
                    BackgroundColor3 = ModernColors.Background,
                    ClipsDescendants = true,
                    Size = UDim2.new(0, 370, 0, 28),
                    AutoButtonColor = false,
                    Font = Enum.Font.Gotham,
                    Text = v,
                    TextColor3 = ModernColors.Text,
                    TextSize = 13
                })

                CreateInstance("UICorner", {
                    Parent = Item,
                    CornerRadius = UDim.new(0, 6)
                })

                Item.MouseEnter:Connect(function()
                    TweenCreate(Item, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Background:Lerp(ModernColors.Accent, 0.1)
                    }):Play()
                end)

                Item.MouseLeave:Connect(function()
                    TweenCreate(Item, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundColor3 = ModernColors.Background
                    }):Play()
                end)

                Item.MouseButton1Click:Connect(function()
                    droptog = false
                    DropdownTitle.Text = text .. " - " .. v
                    pcall(callback, v)
                    Dropdown:TweenSize(UDim2.new(0, 420, 0, 45), Enum.EasingDirection.Out, Enum.EasingStyle.Quart, 0.2, true)
                    TweenCreate(ArrowImg, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        Rotation = 0,
                        ImageColor3 = ModernColors.SubText
                    }):Play()
                end)

                DropItemHolder.CanvasSize = UDim2.new(0, 0, 0, DropLayout.AbsoluteContentSize.Y)
            end
        end

        function tabcontent:Textbox(text, callback)
            local Textbox = CreateInstance("Frame", {
                Name = "Textbox",
                Parent = Tab,
                BackgroundColor3 = ModernColors.Secondary,
                ClipsDescendants = true,
                Size = UDim2.new(0, 420, 0, 45)
            })

            CreateInstance("UICorner", {
                Parent = Textbox,
                CornerRadius = UDim.new(0, 8)
            })

            CreateInstance("UIStroke", {
                Parent = Textbox,
                Color = ModernColors.Border,
                Thickness = 1,
                Transparency = 0.8
            })

            local TextboxTitle = CreateInstance("TextLabel", {
                Name = "TextboxTitle",
                Parent = Textbox,
                BackgroundTransparency = 1,
                Position = UDim2.new(0.05, 0, 0, 0),
                Size = UDim2.new(0.4, 0, 1, 0),
                Font = Enum.Font.Gotham,
                Text = text,
                TextColor3 = ModernColors.Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left
            })

            local TextboxFrame = CreateInstance("Frame", {
                Name = "TextboxFrame",
                Parent = Textbox,
                BackgroundColor3 = ModernColors.Background,
                Position = UDim2.new(0.5, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Size = UDim2.new(0.45, 0, 0.6, 0)
            })

            CreateInstance("UICorner", {
                Parent = TextboxFrame,
                CornerRadius = UDim.new(0, 6)
            })

            CreateInstance("UIStroke", {
                Parent = TextboxFrame,
                Color = ModernColors.Border,
                Thickness = 1
            })

            local TextBox = CreateInstance("TextBox", {
                Parent = TextboxFrame,
                BackgroundTransparency = 1,
                Size = UDim2.new(0.9, 0, 0.9, 0),
                Position = UDim2.new(0.5, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Font = Enum.Font.Gotham,
                Text = "",
                PlaceholderColor3 = ModernColors.SubText,
                TextColor3 = ModernColors.Text,
                TextSize = 13,
                TextWrapped = true,
                TextXAlignment = Enum.TextXAlignment.Center,
                TextYAlignment = Enum.TextYAlignment.Center,
                ClearTextOnFocus = false
            })

            TextBox.Focused:Connect(function()
                TweenCreate(TextboxFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Background:Lerp(ModernColors.Accent, 0.1)
                }):Play()
            end)

            TextBox.FocusLost:Connect(function(enterPressed)
                TweenCreate(TextboxFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    BackgroundColor3 = ModernColors.Background
                }):Play()
                
                if enterPressed and #TextBox.Text > 0 then
                    pcall(callback, TextBox.Text)
                end
            end)
        end

        return tabcontent
    end
    
    return tabhold
end

return lib